from __future__ import annotations

def get(path) -> interface:
   pass

class interface:
   def add_float_slider(this, name, defaultValue, min, max):
      pass

   def add_int_slider(this, name, defaultValue, min, max):
      pass

   def clear_controls(this, ):
      pass

